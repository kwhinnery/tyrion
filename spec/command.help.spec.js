var tyrion = require('../lib');

describe('tyrion\'s SMS message parser', function() {
    it('should print out a list of all available commands', function() {
        expect(true).toBeTruthy();
    });
});